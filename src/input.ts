import { screenToWorld, worldToScreen } from './main';
// Placeholder: main file will export screenToWorld/worldToScreen for reuse.

export function setupInput(canvas: HTMLCanvasElement){
  // actual handlers remain in main - this module exists for future split.
}
